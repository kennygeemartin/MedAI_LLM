DATABASE_URL=sqlite:///./medai.db
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=
APP_ENV=development
# Optional Groq free-plan API. Keep the account on Free to avoid paid usage.
GROQ_API_KEY=
GROQ_MODEL=openai/gpt-oss-20b
# Bootstrap an admin using: python -m backend.manage create-admin
# Optional private inference service. Both settings must be supplied together.
INFERENCE_URL=
INFERENCE_API_KEY=
# Inference service only: accept the MedGemma license before downloading.
HF_TOKEN=
MODEL_ID=google/medgemma-4b-it
